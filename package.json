{
  "name": "gh-contributions-notifier",
  "version": "1.0.0",
  "description": "Skip leg day, not commit day",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Anonyma (www.anonyma.dev)",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.24.0",
    "node-cron": "^3.0.0"
  }
}